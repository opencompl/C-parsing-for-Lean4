if (foo[z] <= x + yz)
    baz = bat + bar;
else if (foo[z] == x + yz)
    baz = bat * bar;
else if (foo[z] >= x + yz)
    baz = bat / bar;
else {
    switch (foo[z] - x - yz)
    {
        foo : bar;
        bat : baz;
        yer : mov;
    }
}
